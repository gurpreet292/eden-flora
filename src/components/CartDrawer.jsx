import { ArrowRight, Check, Minus, Plus, ShoppingBag, Trash2, X } from 'lucide-react'
import { Link } from 'react-router-dom'
import { useStore } from '../hooks/useStore'

const CartDrawer = ({ open, onClose }) => {
  const { cart, subtotal, updateQuantity, removeFromCart } = useStore()
  return <div className={`fixed inset-0 z-90 transition ${open ? 'pointer-events-auto' : 'pointer-events-none'}`} aria-hidden={!open}><div onClick={onClose} className={`absolute inset-0 bg-forest/25 backdrop-blur-sm transition-opacity ${open ? 'opacity-100' : 'opacity-0'}`} /><aside className={`absolute right-0 top-0 flex h-full w-full max-w-md flex-col bg-ivory p-6 shadow-2xl transition-transform duration-500 sm:p-8 ${open ? 'translate-x-0' : 'translate-x-full'}`} role="dialog" aria-modal="true" aria-label="Garden bag"><div className="flex items-center justify-between border-b border-forest/10 pb-5"><div><p className="text-[10px] font-bold uppercase tracking-[0.24em] text-gold">Your garden bag</p><h2 className="mt-2 font-display text-3xl">Growing nicely.</h2></div><button onClick={onClose} aria-label="Close garden bag" className="grid size-10 place-items-center rounded-full bg-sage"><X size={18} /></button></div><div className="flex-1 overflow-y-auto py-5">{cart.length ? cart.map((item) => <div key={item.id} className="flex gap-4 border-b border-forest/10 py-5 first:pt-0"><img src={`https://images.unsplash.com/${item.image}?auto=format&fit=crop&w=240&q=80`} alt={item.name} className="size-20 rounded-xl object-cover" /><div className="min-w-0 flex-1"><p className="truncate font-display text-xl">{item.name}</p><p className="mt-1 text-xs text-forest/50">${item.price}</p><div className="mt-3 flex items-center justify-between"><div className="flex items-center rounded-full border border-forest/15"><button onClick={() => updateQuantity(item.id, item.quantity - 1)} aria-label="Decrease quantity" className="grid size-7 place-items-center"><Minus size={12} /></button><span className="w-6 text-center text-xs">{item.quantity}</span><button onClick={() => updateQuantity(item.id, item.quantity + 1)} aria-label="Increase quantity" className="grid size-7 place-items-center"><Plus size={12} /></button></div><button onClick={() => removeFromCart(item.id)} aria-label={`Remove ${item.name}`} className="text-forest/40 hover:text-red-800"><Trash2 size={14} /></button></div></div></div>) : <div className="grid h-full place-items-center text-center"><div><ShoppingBag size={24} strokeWidth={1} className="mx-auto text-gold" /><p className="mt-5 font-display text-3xl">Nothing here yet.</p><p className="mt-2 max-w-xs text-sm leading-6 text-forest/55">Choose a living piece and it will appear here.</p></div></div>}</div>{cart.length > 0 && <div className="border-t border-forest/10 pt-5"><div className="flex justify-between text-sm"><span className="text-forest/55">Subtotal</span><span className="font-semibold">${subtotal.toFixed(2)}</span></div><Link to="/cart" onClick={onClose} className="mt-5 flex items-center justify-center gap-3 rounded-full bg-forest px-5 py-4 text-sm font-semibold text-ivory hover:bg-fern">Review garden bag <ArrowRight size={15} /></Link></div>}{!cart.length && <button onClick={onClose} className="flex items-center justify-center gap-2 text-xs font-bold uppercase tracking-[0.16em] text-fern"><Check size={14} /> Close</button>}</aside></div>
}

export default CartDrawer
